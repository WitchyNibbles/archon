# web/public/.gitignore
#
# snapshot.json is committed as a representative sample (validated against
# DashboardViewModelSchema). If you regenerate it from a live run it may
# contain real run IDs — uncomment the line below to gitignore live versions:
#
# snapshot.json
#
# The generated/ directory holds runtime-produced assets (never committed).
generated/
