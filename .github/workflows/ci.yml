name: CI

on:
  workflow_dispatch:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master
  merge_group:

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

env:
  NODE_VERSION: "22"

permissions:
  contents: read

jobs:
  release-overlay:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    env:
      ARCHON_REVIEW_IDENTITY_BINDINGS: .archon/templates/review-identity-bindings.json
      ARCHON_REVIEW_IDENTITY_FIXTURES: .archon/templates/review-identity-adapter.fixture.json

    steps:
      - name: Checkout source
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          persist-credentials: false
      - name: Set up Node
        uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6.4.0
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: npm
      - run: npm ci
      # build:dist compiles src/**/*.ts → dist/**/*.js (esbuild). verify-release-overlay
      # runs the full suite, including tests that assert `npm pack --dry-run` ships a
      # populated dist/ — they hard-fail unless dist/ exists first (same reason unit-tests
      # builds dist below).
      - run: npm run build:dist
      - run: npm run verify:release-overlay

  live-migrations:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    services:
      postgres:
        image: pgvector/pgvector:0.8.2-pg18
        env:
          POSTGRES_DB: archon
          POSTGRES_USER: archon
          POSTGRES_PASSWORD: archon
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U archon -d archon"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 20
    env:
      ARCHON_CORE_DATABASE_URL: postgresql://archon:archon@127.0.0.1:5432/archon
      ARCHON_WORKSPACE_SLUG: ci
      ARCHON_WORKSPACE_NAME: CI Workspace
      ARCHON_PROJECT_SLUG: archon-ci
      ARCHON_PROJECT_NAME: archon-ci
      ARCHON_PROJECT_REPO_PATH: ${{ github.workspace }}

    steps:
      - name: Checkout source
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          persist-credentials: false
      - name: Set up Node
        uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6.4.0
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: npm
      - run: npm ci
      - run: npm run verify:migrations:live

  # NOTE: a windows-setup-smoke job and a property-regressions job were removed here.
  # Both referenced artifacts that do not exist (tests/setup-powershell-smoke.test.ts
  # and an npm "test:properties" script with no property tests), so they could never
  # pass. They are tracked as follow-up initiatives: real Windows install/setup CI
  # coverage, and a property-based regression suite. Re-add each job only once it runs
  # a real, green-able check.

  lint:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Checkout source
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          persist-credentials: false
      - name: Set up Node
        uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6.4.0
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: npm
      - run: npm ci
      # ESLint (flat config, typescript-eslint). Fails on any error; dead imports
      # are error-level so they cannot regress. Unused locals remain warnings.
      - run: npm run lint

  unit-tests:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout source
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          persist-credentials: false
      - name: Set up Node
        uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6.4.0
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: npm
      - run: npm ci
      - run: npm run build
      # build:dist compiles src/**/*.ts → dist/**/*.js (esbuild). Tests that run
      # npm pack --dry-run must see a populated dist/ to make non-vacuous assertions.
      - run: npm run build:dist
      # Runs the full suite under c8 and enforces the coverage ratchet floor
      # (package.json "c8" key). Fails on test failure OR coverage regression.
      - run: npm run check:coverage

  # P1 pack-install smoke: proves the compiled package is self-contained and
  # invocable via the bin without --experimental-strip-types.  Added to
  # required-checks so tarball validation gates merges (all 4 legs: Linux+Windows,
  # Node 22+24).
  pack-install:
    runs-on: ${{ matrix.os }}
    timeout-minutes: 20
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest]
        node-version: ["22", "24"]

    steps:
      - name: Checkout source
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          persist-credentials: false
      - name: Set up Node ${{ matrix.node-version }}
        uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6.4.0
        with:
          node-version: ${{ matrix.node-version }}
          cache: npm
      - run: npm ci
      - name: Build dist
        run: npm run build:dist

      # Pack → install into a clean temp dir → run smoke checks (unix)
      - name: Pack and install in clean temp dir (unix)
        if: runner.os != 'Windows'
        shell: bash
        run: |
          set -euo pipefail
          PACK_DIR="$(mktemp -d)"
          INSTALL_DIR="$(mktemp -d)"

          TARBALL="$(npm pack --pack-destination "$PACK_DIR" 2>/dev/null | tail -1)"
          TARBALL_PATH="$PACK_DIR/$TARBALL"

          cd "$INSTALL_DIR"
          npm init -y --quiet
          npm install --quiet "$TARBALL_PATH"

          # Invoke the compiled bin entry directly (dist/cli/archon-bin.js is the
          # bin target) so the smoke test is identical to the windows leg and does
          # not depend on the npm-generated .bin shim.
          BIN="./node_modules/@witchynibbles/archon/dist/cli/archon-bin.js"

          # archon --help must exit 0
          node "$BIN" --help

          # archon doctor: DB error (exit 1) is acceptable; module-not-found is not
          node "$BIN" doctor || {
            code=$?
            if [ "$code" -ne 1 ]; then
              printf 'archon doctor exited with unexpected code %d\n' "$code" >&2
              exit 1
            fi
          }

          # P4: init --apply smoke — prove the compiled bin routes init to the
          # install CLI, writes devDependencies.archon, and wires archon scripts.
          # Docker is not available in CI so we stub it; init --apply must not call it.
          INIT_DIR="$(mktemp -d)"
          DOCKER_SENTINEL="$INIT_DIR/.docker-called"
          FAKE_BIN_DIR="$(mktemp -d)"
          printf '#!/usr/bin/env bash\nset -euo pipefail\n: > "%s"\nexit 0\n' "$DOCKER_SENTINEL" > "$FAKE_BIN_DIR/docker"
          chmod +x "$FAKE_BIN_DIR/docker"
          printf '{"name":"smoke-project","private":true}\n' > "$INIT_DIR/package.json"
          PATH="$FAKE_BIN_DIR:$PATH" node "$BIN" init --apply --target "$INIT_DIR"

          # Assertions live in scripts/ci/assert-init-applied.mjs (shared with the
          # windows leg) — no inline heredoc in the YAML block.
          node "$GITHUB_WORKSPACE/scripts/ci/assert-init-applied.mjs" "$INIT_DIR/package.json" "$DOCKER_SENTINEL"

          # S1 capability engine / verify --json (D-C11 BOTH: CI compiled-bin leg).
          # Proves the compiled bin runs the L1 engine and finds mcp-archon + playwright.
          # This catches the #140 class in the SHIPPED tarball (not just unit tests).
          # HIGH-1: init --apply does not create .env.archon (DB is BYO); seed a stub
          # so probeDatabaseUrl returns ok and verify --json exits 0 on the happy path.
          printf 'ARCHON_CORE_DATABASE_URL=postgres://archon:archon@localhost:5432/archon\n' > "$INIT_DIR/.env.archon"
          node "$BIN" verify --json --target "$INIT_DIR" > "$INIT_DIR/verify-json-output.json"
          node "$GITHUB_WORKSPACE/scripts/ci/assert-verify-json.mjs" "$INIT_DIR"

          # Assert dist tree cleanliness, shipped-script cleanliness, and no install
          # lifecycle hooks. The inspection logic lives in scripts/ci/*.mjs (shared
          # verbatim with the windows leg) so the rules live in ONE place. $GITHUB_WORKSPACE
          # is the repo checkout; the arg is the installed package under the temp dir.
          node "$GITHUB_WORKSPACE/scripts/ci/assert-dist-clean.mjs" "$INSTALL_DIR/node_modules/@witchynibbles/archon/dist"
          node "$GITHUB_WORKSPACE/scripts/ci/assert-scripts-clean.mjs" "$INSTALL_DIR/node_modules/@witchynibbles/archon/scripts"
          node "$GITHUB_WORKSPACE/scripts/ci/assert-no-lifecycle-hooks.mjs" "$INSTALL_DIR/node_modules/@witchynibbles/archon/package.json"

      # Pack → install into a clean temp dir → run smoke checks (windows)
      # Inspection logic is shared with the unix leg via scripts/ci/*.mjs (no inline
      # PowerShell here-strings — their column-0 closing delimiter is incompatible
      # with an indented YAML `run: |` block and previously broke this workflow).
      - name: Pack and install in clean temp dir (windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          $ErrorActionPreference = "Stop"
          $packDir  = Join-Path ([System.IO.Path]::GetTempPath()) ([System.Guid]::NewGuid().ToString())
          $installDir = Join-Path ([System.IO.Path]::GetTempPath()) ([System.Guid]::NewGuid().ToString())
          New-Item -ItemType Directory -Path $packDir  | Out-Null
          New-Item -ItemType Directory -Path $installDir | Out-Null

          $tarball = npm pack --pack-destination $packDir 2>$null | Select-Object -Last 1
          $tarballPath = Join-Path $packDir $tarball

          Push-Location $installDir
          npm init -y --quiet
          npm install --quiet $tarballPath

          # Invoke the compiled bin entry directly. The npm-generated .bin/archon
          # shim is an extensionless POSIX script on Windows that `node` cannot
          # execute; dist/cli/archon-bin.js is the bin target and runs identically
          # on every platform. Node accepts forward slashes on Windows.
          $bin = "node_modules/@witchynibbles/archon/dist/cli/archon-bin.js"

          # archon --help must exit 0
          node $bin --help
          if ($LASTEXITCODE -ne 0) { exit 1 }

          # archon doctor: DB error (exit 1) acceptable; anything else is a failure
          node $bin doctor
          $code = $LASTEXITCODE
          if ($code -ne 0 -and $code -ne 1) {
            Write-Error "archon doctor exited with unexpected code $code"
            exit 1
          }

          # P4: init --apply smoke — prove the compiled bin routes init to the
          # install CLI, writes devDependencies.archon, and wires archon scripts.
          $initDir = Join-Path ([System.IO.Path]::GetTempPath()) ([System.Guid]::NewGuid().ToString())
          New-Item -ItemType Directory -Path $initDir | Out-Null
          $dockerSentinel = Join-Path $initDir ".docker-called"
          Set-Content -Path (Join-Path $initDir "package.json") -Value '{"name":"smoke-project","private":true}'
          # Fake docker on PATH (analogous to the unix bash stub): a docker.cmd that
          # records the sentinel when invoked, so the docker-not-called assertion is
          # genuine on Windows too. exit /b 0 so init treats it as available.
          $fakeBin = Join-Path ([System.IO.Path]::GetTempPath()) ([System.Guid]::NewGuid().ToString())
          New-Item -ItemType Directory -Path $fakeBin | Out-Null
          $dockerCmd = "@echo off`r`necho called> `"$dockerSentinel`"`r`nexit /b 0"
          Set-Content -Path (Join-Path $fakeBin "docker.cmd") -Value $dockerCmd -Encoding ASCII
          $env:PATH = "$fakeBin;$env:PATH"
          node $bin init --apply --target $initDir
          if ($LASTEXITCODE -ne 0) { Write-Error "init --apply failed"; exit 1 }

          # Assertions live in scripts/ci/assert-init-applied.mjs (shared with the
          # unix leg) — single source of truth, no inline here-string in the YAML.
          node "$env:GITHUB_WORKSPACE/scripts/ci/assert-init-applied.mjs" (Join-Path $initDir "package.json") $dockerSentinel
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }

          # S1 capability engine / verify --json (D-C11 BOTH: CI compiled-bin leg).
          # HIGH-1: init --apply does not create .env.archon (DB is BYO); seed a stub
          # so probeDatabaseUrl returns ok and verify --json exits 0 on the happy path.
          Set-Content -Path (Join-Path $initDir ".env.archon") -Value "ARCHON_CORE_DATABASE_URL=postgres://archon:archon@localhost:5432/archon" -Encoding utf8
          $verifyJsonOutput = Join-Path $initDir "verify-json-output.json"
          node $bin verify --json --target $initDir | Out-File -FilePath $verifyJsonOutput -Encoding utf8
          if ($LASTEXITCODE -ne 0) { Write-Error "verify --json exited with unexpected code $LASTEXITCODE"; exit 1 }
          node "$env:GITHUB_WORKSPACE/scripts/ci/assert-verify-json.mjs" $initDir
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }

          # Dist/scripts/package inspection. Same scripts/ci/*.mjs the unix leg runs
          # (single source of truth). Node accepts forward slashes on Windows, so
          # $env:GITHUB_WORKSPACE composes cleanly. Each check must exit 0.
          $ci = "$env:GITHUB_WORKSPACE/scripts/ci"
          node "$ci/assert-dist-clean.mjs" (Join-Path $installDir "node_modules\@witchynibbles\archon\dist")
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
          node "$ci/assert-scripts-clean.mjs" (Join-Path $installDir "node_modules\@witchynibbles\archon\scripts")
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
          node "$ci/assert-no-lifecycle-hooks.mjs" (Join-Path $installDir "node_modules\@witchynibbles\archon\package.json")
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }

          Pop-Location

  # P5 publish guard: verify the package is in a publishable state on every PR.
  # Complements pack-install (which proves tarball install + smoke) by validating
  # npm registry metadata (name, version, access, files list) via --dry-run.
  # Runs build:dist + build:types explicitly so the dist/ is populated before the
  # dry-run; --ignore-scripts prevents prepublishOnly from running a second build.
  publish-dry-run:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout source
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          persist-credentials: false
      - name: Set up Node
        uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6.4.0
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: npm
      - run: npm ci
      - name: Build dist and types
        run: npm run build:dist && npm run build:types
      - name: Verify package is publishable (dry-run)
        run: npm publish --dry-run --ignore-scripts --access public

  required-checks:
    if: ${{ always() }}
    needs:
      - release-overlay
      - live-migrations
      - lint
      - unit-tests
      - pack-install
      - publish-dry-run
    runs-on: ubuntu-latest
    timeout-minutes: 5

    steps:
      - name: Enforce required job results
        env:
          RELEASE_OVERLAY_RESULT: ${{ needs.release-overlay.result }}
          LIVE_MIGRATIONS_RESULT: ${{ needs.live-migrations.result }}
          LINT_RESULT: ${{ needs.lint.result }}
          UNIT_TESTS_RESULT: ${{ needs.unit-tests.result }}
          PACK_INSTALL_RESULT: ${{ needs.pack-install.result }}
          PUBLISH_DRY_RUN_RESULT: ${{ needs.publish-dry-run.result }}
        run: |
          echo "release-overlay: ${RELEASE_OVERLAY_RESULT}"
          echo "live-migrations: ${LIVE_MIGRATIONS_RESULT}"
          echo "lint: ${LINT_RESULT}"
          echo "unit-tests: ${UNIT_TESTS_RESULT}"
          echo "pack-install: ${PACK_INSTALL_RESULT}"
          echo "publish-dry-run: ${PUBLISH_DRY_RUN_RESULT}"

          test "${RELEASE_OVERLAY_RESULT}" = "success"
          test "${LIVE_MIGRATIONS_RESULT}" = "success"
          test "${LINT_RESULT}" = "success"
          test "${UNIT_TESTS_RESULT}" = "success"
          test "${PACK_INSTALL_RESULT}" = "success"
          test "${PUBLISH_DRY_RUN_RESULT}" = "success"
