#!/usr/bin/env bash
set -euo pipefail

repo_root="$(git rev-parse --show-toplevel 2>/dev/null || pwd)"
cd "$repo_root"

bash scripts/check-archon-branch-name.sh
bash scripts/check-archon-git-guard.sh
